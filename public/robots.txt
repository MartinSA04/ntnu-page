# Two pages, and they link to each other, so the sitemap is a formality. It was
# the only way in while the build emitted 5470 course pages nothing
# server-rendered linked to; those are deleted (PRODUCT D10).
#
# The host below MUST match `site` in astro.config.mjs. It is the real one as
# of 2026-08-18, not a placeholder, and the two have to move together if it
# ever changes. tests/site/discoverability.test.ts asserts they agree.
User-agent: *
Allow: /

# JSON for the site's own scripts, not content — nothing to index, and the
# worker pays an upstream fetch for a cold one.
Disallow: /api/

Sitemap: https://ntnu.martinsundal.no/sitemap.xml
